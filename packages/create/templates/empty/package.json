{
  "name": "__app-name__",
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@gnim-js/gtk4": "^2.0.0-beta.23",
    "gnim": "^2.0.0-beta.23"
  },
  "scripts": {
    "types": "gnim types/*% if vala %*/ -d build/lib/*% endif %*/",
    "dev": "gnim dev --id __app-id__ src/main.ts",
    "build": "rm -rf dist ; meson setup build --wipe --prefix \"$(pwd)/dist\" ; meson install -C build"
  }
}
