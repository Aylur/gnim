{
  "name": "__app-name__",
  "type": "module",
  "dependencies": {
    "@gnim-js/gtk4": "^2.0.0-beta.6",
    "gnim": "^2.0.0-beta.6"
  },
  "scripts": {
    "types": "gnim types",
    "dev": "gnim dev src/main.ts",
    "build": "rm -rf dist ; meson setup build --wipe --prefix \"$(pwd)/dist\" ; meson install -C build"
  }
}
