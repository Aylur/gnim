{
    "private": true,
    "type": "module",
    "author": "Aylur",
    "license": "MIT",
    "repository": "github:Aylur/gnim",
    "funding": {
        "type": "kofi",
        "url": "https://ko-fi.com/aylur"
    },
    "scripts": {
        "clean": "git clean -Xdf",
        "types": "./scripts.nu types",
        "ci": "./scripts.nu ci",
        "build": "pnpm -r run --parallel build",
        "test": "vitest run",
        "lint": "pnpm -r run --parallel lint",
        "fmt": "pnpm -r run --parallel fmt",
        "fmt:check": "pnpm -r run --parallel fmt:check",
        "typecheck": "pnpm -r run --parallel typecheck",
        "docs:dev": "vitepress dev docs",
        "docs:build": "vitepress build docs"
    },
    "devDependencies": {
        "@vercel/analytics": "^2.0.1",
        "devicon": "^2.17.0",
        "girgen": "^0.3.20",
        "nushell": "latest",
        "prettier": "^3.9.6",
        "vitepress": "2.0.0-alpha.16",
        "vitepress-plugin-llmstxt": "^0.5.2",
        "vitest": "^4.1.10",
        "vue": "^3.5.40"
    },
    "prettier": {
        "semi": false,
        "tabWidth": 4,
        "quoteProps": "consistent",
        "trailingComma": "all",
        "printWidth": 100,
        "experimentalTernaries": false,
        "overrides": [
            {
                "files": "**/*.md",
                "options": {
                    "tabWidth": 2,
                    "printWidth": 80,
                    "proseWrap": "always"
                }
            }
        ]
    }
}
